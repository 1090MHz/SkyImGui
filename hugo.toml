baseURL = 'https://skyimgui.netlify.app/'
languageCode = 'en-us'
title = 'ImGui Theme Studio'
theme = 'imgui-studio'
disableKinds = ['taxonomy', 'term']

[outputs]
  home = ['HTML']

[params]
  description = 'Interactive ImGui theme visualizer and generator inspired by airline brand colors'
  author = 'SkyImGui'

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
